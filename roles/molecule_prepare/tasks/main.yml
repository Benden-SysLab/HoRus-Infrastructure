---
- name: Update apt cache
  ansible.builtin.raw: apt-get update
  changed_when: false

- name: Install Python 3, python3-apt, and sudo
  ansible.builtin.raw: apt-get install -y python3 python3-apt sudo ca-certificates
  changed_when: false
