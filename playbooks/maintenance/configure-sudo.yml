---
- name: "Operational: Configure Sudo Privileges"
  hosts: all
  become: true
  gather_facts: true
  vars_files:
    - "{{ playbook_dir }}/../../inventory/group_vars/vault_inventory.yml"
  tasks:
    - name: Run Users/Sudo tasks
      ansible.builtin.include_role:
        name: common
        tasks_from: users
