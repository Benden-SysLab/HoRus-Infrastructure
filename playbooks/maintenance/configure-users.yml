---
- name: "Operational: Configure Users"
  hosts: all
  become: true
  gather_facts: true
  vars_files:
    - "{{ playbook_dir }}/../../inventory/group_vars/vault_inventory.yml"
  tasks:
    - name: Run Users task
      ansible.builtin.include_role:
        name: common
        tasks_from: users
